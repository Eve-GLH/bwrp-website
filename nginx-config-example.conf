# Nginx configuration for removing .html extensions
# Add this to your server block in nginx.conf

location / {
    try_files $uri $uri.html $uri/ =404;
}

# Redirect .html URLs to clean URLs
location ~ \.html$ {
    return 301 $scheme://$host$uri;
}